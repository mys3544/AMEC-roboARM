# Stage 2 of the vision image install (see vision.Dockerfile).
#
# Installed WITH uv's dependency resolution, so sub-deps (python-dateutil,
# fonttools, ...) come along. Nothing here pulls torch / torchvision / opencv,
# and the base's numpy 1.26 + pillow 10.4 satisfy the ones that care, so uv
# leaves the CUDA stack from stage 1 alone.

# --- ultralytics deps the l4t-pytorch base does not ship ---
# pyyaml is import-time for `import ultralytics`; matplotlib and polars are on
# the predict/plotting path (ultralytics >= 8.3 uses polars, not pandas, and
# its own progress bar, not tqdm). scipy is lazy, in the trackers and metrics.
pyyaml>=6.0
scipy>=1.11
matplotlib>=3.7
polars>=1.0

# For `tools/export_engine.py`: ultralytics' ONNX->TensorRT path simplifies the
# graph with onnxslim before handing it to the (base-image) tensorrt bindings.
# onnx itself and the tensorrt python package are already in the l4t base.
onnxslim>=0.1.48

# What the CLIP fork (stage 1) imports at runtime. All pure Python.
ftfy>=6.0
regex>=2023.0
wcwidth>=0.2

# The HTTP layer is the standard library (vision_service/serve.py): nothing to install.
